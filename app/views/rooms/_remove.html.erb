<%= form_for(current_user.memberships.find_by_participant_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Remove", class: "btn btn-large" %>
<% end %>